<!DOCTYPE html>
<html lang="pl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Strona główna</title>
    <link rel="stylesheet" href="/css/assignModel.css">
</head>

<body>
    <header>

        <a href="/index" class="logo">
            <img src="/images/logo.png" alt="Logo firmy">
        </a>

        <div class="menu-bar">
            <div class="menu-item">
                <a href="{{ path('app_stan') }}"> <button id="stanMagazynowyBtn">STAN MAGAZYNOWY</button> </a>
            </div>
            <div class="menu-item">
                <a href="{{ path('app_sprzet') }}"> <button id="sprzetBtn">SPRZĘT</button> </a>
            </div>
            <div class="menu-item">
                <a href="{{ path('app_users') }}"> <button id="uzytkownicyBtn">UŻYTKOWNICY</button> </a>
            </div>
        </div>

        <button class="logout" id="logouserBtn"><img src="logouser.png" alt="Ikona użytkownika"></button>
    </header>

    <main class="main-content">
        <h1>MODEL KTÓRY PRZYPISUJESZ: {{ chosenModel.nazwa }} </h1>
        <form class="form-content" method="POST">
            <div class="form-group">
                <label for="sprzet">WYBIERZ SPRZĘT</label>
                <select id="sprzet" name="sprzet">
                    {% for sprzet in sprzety %}
                        <option value="{{ sprzet.id }}">{{ sprzet.hostname }}</option>
                    {% endfor %}
                </select>

            </div>
            <div class="form-group">
                <label for="uzytkownik">WYBIERZ UŻYTKOWNIKA</label>
                <select id="uzytkownik" name="uzytkownik">
                    {% for user in users %}
                        <option value="{{ user.id }}">{{ user.imieNazwisko }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="form-group">
                <label for="data-wydania">DATA WYDANIA</label>
                <input type="date" id="data-wydania" name="data-wydania">
            </div>
            <button id="dodajBtn" type="submit">PRZYPISZ</button>
        </form>
    </main>

    <div id="myModal" class="modal">
        <div class="modal-content">
            <span class="close">&times;</span>
            <button>Puste</button>
            <button id="logoutBtn">Wyloguj się</button>
        </div>
    </div>

    <script src="navi.js"></script>
    <script src="script.js"></script>
    <script src="dodaj.js"></script>

</body>

</html>